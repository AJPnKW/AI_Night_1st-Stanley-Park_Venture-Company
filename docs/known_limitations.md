# Known Limitations

- The consent form requires a real external endpoint before live online use.
- The project uses direct HTML duplication rather than a templating engine to keep GitHub Pages maintenance simple.
- Low-profile leader and facilitator pages are hidden by routing convention, not authentication.
- Final event-specific contact details are not embedded until the organizer configures them.

